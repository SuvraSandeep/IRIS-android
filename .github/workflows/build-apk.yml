name: Build PULSE Control v8.7.1 Standard Debug APK

run-name: PULSE Control v8.7.1 Standard Debug • ${{ github.ref_name }} • ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

permissions:
  contents: read

# Single source of truth for this workflow. These values must match
# app/build.gradle.kts in the v8.7.1 source archive.
env:
  PULSE_VERSION: "8.7.1"
  PULSE_VERSION_CODE: "179"
  PULSE_SOURCE_ARCHIVE: "PULSE-Control-v8.7.1-debug-source.zip"
  PULSE_APK_NAME: "PULSE-Control-v8.7.1-standard-debug.apk"

jobs:
  build-standard-debug:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Locate PULSE Control v8.7.1 standard project
        shell: bash
        run: |
          set -euo pipefail

          project_dir="$GITHUB_WORKSPACE"
          archive="$GITHUB_WORKSPACE/PULSE-Control-v8.7.1-debug-source.zip"

          if [[ -f "$archive" ]]; then
            echo "Using PULSE Control v8.7.1 source archive: $archive"
            extract_dir="$RUNNER_TEMP/pulse-control-v8.7.1-debug-source"
            mkdir -p "$extract_dir"
            unzip -q "$archive" -d "$extract_dir"

            wrapper="$(find "$extract_dir" -type f -name gradlew -print -quit)"
            if [[ -z "$wrapper" ]]; then
              echo "::error::The ZIP does not contain an Android Gradle project."
              exit 1
            fi
            project_dir="$(dirname "$wrapper")"
          elif [[ -f "$project_dir/gradlew" ]]; then
            echo "Source archive not present; using the extracted repository-root project."
          else
            echo "::error::Expected PULSE-Control-v8.7.1-debug-source.zip at repository root or an extracted Android project."
            exit 1
          fi

          echo "PROJECT_DIR=$project_dir" >> "$GITHUB_ENV"
          echo "Android project: $project_dir"

      - name: Verify PULSE Control v8.7.1 source
        shell: bash
        run: |
          set -euo pipefail

          build_file="$PROJECT_DIR/app/build.gradle.kts"
          if [[ ! -f "$build_file" ]] ||
             ! grep -q 'versionCode = 179' "$build_file" ||
             ! grep -q 'versionName = "8.7.1"' "$build_file"; then
            echo "::error::The selected project is not PULSE Control v8.7.1."
            exit 1
          fi

          if [[ -f "$PROJECT_DIR/tools/verify_project.py" ]]; then
            python3 "$PROJECT_DIR/tools/verify_project.py"
          fi
          echo "Confirmed PULSE Control v8.7.1 source."

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android 36 platform
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Build and test Standard debug APK
        shell: bash
        run: |
          set -o pipefail
          cd "$PROJECT_DIR"
          chmod +x gradlew
          ./gradlew --no-daemon --console=plain \
            :app:compileStandardDebugKotlin \
            :app:testStandardDebugUnitTest \
            :app:lintStandardDebug \
            :app:assembleStandardDebug \
            --stacktrace 2>&1 | tee "$GITHUB_WORKSPACE/gradle-build.log"

      - name: Verify Play Protect sensitive declarations are absent
        shell: bash
        run: |
          set -euo pipefail
          apk="$PROJECT_DIR/app/build/outputs/apk/standard/debug/app-standard-debug.apk"
          manifest_text="$(apkanalyzer manifest print "$apk")"
          if grep -Eq 'BIND_NOTIFICATION_LISTENER_SERVICE|PulseNotificationListenerService|BIND_ACCESSIBILITY_SERVICE|READ_SMS|RECEIVE_SMS' <<< "$manifest_text"; then
            echo "::error::Standard APK still declares a Play Protect sensitive capability."
            exit 1
          fi
          echo "Standard APK contains no SMS, Notification Listener, or Accessibility declaration."

      - name: Collect Standard debug APK
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$GITHUB_WORKSPACE/dist"
          cp "$PROJECT_DIR/app/build/outputs/apk/standard/debug/app-standard-debug.apk" \
            "$GITHUB_WORKSPACE/dist/$PULSE_APK_NAME"
          sha256sum "$GITHUB_WORKSPACE/dist/$PULSE_APK_NAME" \
            > "$GITHUB_WORKSPACE/dist/$PULSE_APK_NAME.sha256"

      - name: Show compiler errors
        if: failure()
        shell: bash
        run: |
          if [[ -f "$GITHUB_WORKSPACE/gradle-build.log" ]]; then
            grep -E '(^e: |\.kt:[0-9]+:[0-9]+|error: |Error: |Lint found|FAILED)' \
              "$GITHUB_WORKSPACE/gradle-build.log" || true
          fi

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: PULSE-Control-v8.7.1-standard-debug-build-log
          path: gradle-build.log
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Standard debug APK
        uses: actions/upload-artifact@v7
        with:
          name: PULSE-Control-v8.7.1-standard-debug-APK
          path: dist
          if-no-files-found: error
          retention-days: 30
