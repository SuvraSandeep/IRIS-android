/.gradle/
/.idea/
**/build/
*.iml
local.properties
*.jks
*.keystore
!debug.keystore

# Vosk model — downloaded at build time by CI, not committed
app/src/main/assets/model-en-us/
# Gemma LLM model — downloaded at build time, not committed
app/src/main/assets/sherpa-models/
